# UnicornRanch - Landing Page (Nuxt)

Exposed because nothing to hide here and free GitHub Pages.

## Indexation

To enable/disable indexation (basically robots.txt and meta tags), change
`NUXT_SITE_INDEXABLE=true` in `.env`.
